# TCP 서버 실습 학습 가이드

## 프로젝트 구조

```
tcp-practice/
├── src/
│   ├── tcp_server.js   # TCP 서버 (메인 실습 파일)
│   └── tcp_client.js   # TCP 클라이언트 (테스트용)
├── static/
│   ├── index.html      # 프로젝트 소개 페이지
│   ├── cors.html       # CORS 개념 설명
│   └── data.json       # 서버 설정 샘플 데이터
├── package.json
└── 학습가이드.md
```

---

## 실행 방법

### 1. 서버 실행
```bash
node src/tcp_server.js
# 출력: Binded - TCP Server listening on port 8080
```

### 2. 클라이언트로 테스트 (새 터미널)
```bash
node src/tcp_client.js
# 출력: Connected to server
# 출력: Server response: Echo: Hello, TCP Server!
```

### 3. telnet으로 직접 테스트 (Windows)
```bash
# Windows 기능에서 "Telnet 클라이언트" 활성화 후
telnet 127.0.0.1 8080
```

---

## 학습 로드맵

### STEP 1 — 네트워크 기초 (이론)
| 주제 | 핵심 내용 |
|------|-----------|
| OSI 7계층 | 각 계층의 역할과 프로토콜 이해 |
| TCP vs UDP | 연결형 vs 비연결형, 신뢰성 차이 |
| IP & Port | 주소 체계, 포트 번호의 역할 |
| 3-Way Handshake | TCP 연결 수립 과정 (SYN → SYN-ACK → ACK) |
| 소켓(Socket) | 네트워크 통신의 엔드포인트 개념 |

### STEP 2 — Node.js `net` 모듈 (이 프로젝트 핵심)
| 주제 | 관련 코드 |
|------|-----------|
| `net.createServer()` | 서버 소켓 생성 |
| `server.listen(port)` | 포트 바인딩 |
| `onConnect(client)` 콜백 | 클라이언트 연결 이벤트 처리 |
| `client.on('data')` | 데이터 수신 이벤트 |
| `buffer.toString()` | Buffer → 문자열 변환 |
| `client.write()` | 클라이언트에 데이터 전송 |
| `client.end()` | 연결 종료 |

### STEP 3 — Buffer & Encoding
- Node.js에서 네트워크 데이터는 항상 `Buffer` 형태로 수신됨
- `buffer.toString()` → UTF-8 (기본값)
- `buffer.toString('hex')` → 16진수 출력
- `Buffer.from('text')` → 문자열을 Buffer로 변환

### STEP 4 — HTTP 메시지 파싱 (`getHttpMessage`)

TCP로 받은 raw 데이터를 직접 파싱해 HTTP 요청 구조를 이해하는 단계.

#### HTTP 요청 메시지 구조
```
GET / HTTP/1.1\r\n          ← start line
Host: 127.0.0.1:8080\r\n    ← header
\r\n                         ← 헤더 끝 구분자 (빈 줄)
(body)                       ← GET은 보통 없음
```

#### 구현 코드
```javascript
function getHttpMessage(data) {
    const lines = data.split('\r\n');
    const start_line = lines[0];
    const [method, url, version] = start_line.split(' ');
    const headers = {};

    for (let i=1; i<lines.length; i++) {
        const line = lines[i];
        const [k,v] = line.split(': ');
        headers[k] = v;
    }

    return {method, url, headers};
}
```

#### 공부 포인트

| 포인트 | 설명 |
|--------|------|
| `split('\r\n')` | HTTP 스펙(RFC 7230)은 줄 끝을 CRLF로 정의 — `\n`만 쓰면 안 됨 |
| 구조 분해 할당 | `const [method, url, version] = ...` — 배열을 순서대로 변수에 꺼내는 ES6 문법 |
| `split(': ')` | 헤더 파싱 시 첫 번째 `: ` 기준으로 key/value 분리 |
| `{method, url, headers}` | 객체 단축 속성명 — key와 변수명이 같으면 생략 가능 |

#### 이 함수의 한계 (다음 단계 힌트)
- 빈 줄 이후 **body**를 파싱하지 않음
- 헤더 값에 `: `가 포함되면 (`Date: Mon, 18 May 2026`) 잘못 파싱됨
- 개선 방법: `indexOf(': ')`로 첫 번째 콜론 위치만 기준으로 분리

```javascript
// 개선된 헤더 파싱
const idx = line.indexOf(': ');
headers[line.slice(0, idx)] = line.slice(idx + 2);
```

---

### STEP 5 — 이벤트 기반 프로그래밍 (EventEmitter)
- Node.js는 이벤트 루프 기반으로 동작
- `on('data', callback)`, `on('end', callback)`, `on('error', callback)`
- 비동기 I/O 처리 방식 이해

### STEP 5 — Host 헤더 기반 URL 라우팅

파싱한 HTTP 메시지의 `headers.Host`를 보고 응답을 분기하는 단계.

#### 구현 코드
```javascript
const req = getHttpMessage(data);

// soma.com:8080 => hello soma (HTML 응답)
if (req.headers.Host == 'soma.com:8080') {
    const body = '<h1>hello soma</h1>';
    client.write(`HTTP/1.1 200 OK\r\nContent-Type: text/html\r\nContent-Length:${body.length}\r\n\r\n${body}`);
// foo.com:8080 => hello foo
} else if (req.headers.Host == 'foo.com:8080') {
    client.write('HTTP/1.1 200 OK\r\nContent-Length:9\r\n\r\nhello foo');
// any
} else {
    client.write('HTTP/1.1 200 OK\r\nContent-Length:5\r\n\r\nhello');
}
```

#### 분기 구조

| Host 헤더 | 응답 body |
|-----------|-----------|
| `soma.com:8080` | `hello soma` |
| `foo.com:8080` | `hello foo` |
| 그 외 | `hello` |

#### 공부 포인트

| 포인트 | 설명 |
|--------|------|
| `Host` 헤더 | HTTP/1.1에서 필수 헤더 — 같은 IP/포트에서 여러 도메인을 구분하는 가상 호스팅의 원리 |
| HTTP 응답 형식 | `HTTP/1.1 200 OK\r\n헤더\r\n\r\nbody` — 요청과 동일하게 CRLF 사용 |
| `Content-Type: text/html` | 브라우저가 body를 HTML로 렌더링하도록 지시 — 없으면 plain text로 표시 |
| `Content-Length` | body의 바이트 수를 정확히 명시해야 클라이언트가 데이터 끝을 알 수 있음 — `body.length`로 자동 계산 |

#### 테스트 방법
```bash
# Host 헤더를 직접 지정해서 curl로 테스트
curl -H "Host: soma.com:8080" http://127.0.0.1:8080
curl -H "Host: foo.com:8080" http://127.0.0.1:8080
curl http://127.0.0.1:8080
```

---

### STEP 5-2 — HTTP 리다이렉트 (`/redirect` 구현)

`soma.com:8080/redirect` 요청 시 `foo.com:8080`으로 브라우저를 이동시키는 단계.

#### 구현 코드
```javascript
if (req.url == "/redirect") {
  client.write("HTTP/1.1 301 Moved Permanently\r\nLocation: http://foo.com:8080/\r\nContent-Length:0\r\n\r\n");
  return; // 아래 hello soma 응답이 이어서 write되는 것을 방지
}
```

#### 리다이렉트 관련 HTTP 상태 코드 비교

| 코드 | 이름 | 용도 | 메서드 유지 |
|------|------|------|------------|
| **301** | Moved Permanently | 영구 리다이렉트 (이사) | GET으로 변환될 수 있음 |
| **302** | Found | 임시 리다이렉트 (로그인 후 복귀 등) | GET으로 변환될 수 있음 |
| **303** | See Other | POST 처리 후 결과 페이지로 이동 | GET으로 변환 |
| **307** | Temporary Redirect | 임시 리다이렉트 | 원래 메서드 유지 |
| **308** | Permanent Redirect | 영구 리다이렉트 | 원래 메서드 유지 |

> **304는 리다이렉트가 아님** — `304 Not Modified`는 캐싱 응답.  
> 브라우저가 `If-Modified-Since` / `ETag` 헤더와 함께 조건부 요청을 보낼 때,  
> 서버가 "변경 없음, 캐시 그대로 써도 됨"을 알리는 코드. `Location` 헤더도 없고 이동도 없음.

#### 동작 흐름
```
브라우저 → soma.com:8080/redirect
    │
    ▼
서버: 301 + Location: http://foo.com:8080/
    │
    ▼
브라우저 → foo.com:8080/ (자동 재요청)
    │
    ▼
서버: 200 OK + "hello foo"
```

#### 공부 포인트

| 포인트 | 설명 |
|--------|------|
| `Location` 헤더 | 리다이렉트 목적지 URL — 없으면 브라우저가 어디로 가야 할지 모름 |
| `Content-Length: 0` | body가 없는 응답도 `Content-Length: 0`을 명시하는 것이 안전 |
| `return` 필요 | write 후 return 없으면 아래 코드가 이어서 실행되어 응답이 두 번 전송됨 |

#### 테스트 방법
```bash
# -L 옵션: 리다이렉트 자동 추적
curl -L -H "Host: soma.com:8080" http://127.0.0.1:8080/redirect

# -v 옵션: 301 응답과 Location 헤더 직접 확인
curl -v -H "Host: soma.com:8080" http://127.0.0.1:8080/redirect
```

---

### STEP 5-3 — 정적 파일 서빙 (`fs.createReadStream` + `pipe`)

`foo.com:8080/index.html` 요청 시 디스크에 있는 HTML 파일을 그대로 클라이언트에 스트리밍하는 단계.

#### 구현 코드
```javascript
const fs = require("fs");
const path = require("path");

// foo.com:8080 분기 내부
if (req.url == "/index.html") {
  const filePath = path.join(__dirname, "../static/index.html");
  fs.stat(filePath, (err, stats) => {
    client.write(`HTTP/1.1 200 OK\r\nContent-Type: text/html\r\nContent-Length:${stats.size}\r\n\r\n`);
    const readable = fs.createReadStream(filePath);
    readable.pipe(client, { end: false });
  });
  return;
}
client.write("HTTP/1.1 200 OK\r\nContent-Length:9\r\n\r\nhello foo");
```

#### 강의 코드와의 차이점

강의에서는 Content-Length를 `74`로 하드코딩했지만 이 프로젝트는 파일 크기가 다르므로 `fs.stat`으로 실제 바이트 수를 동적으로 계산함.

#### 핵심 개념: Stream과 pipe

```
파일 (Readable Stream)
    │
    ▼  readable.pipe(client)
소켓 (Writable Stream)
```

파일 전체를 메모리에 올리지 않고 **청크(chunk) 단위**로 읽어서 바로 소켓에 씀.

#### 공부 포인트

| 포인트 | 설명 |
|--------|------|
| `fs.stat` | 파일 메타데이터 조회 — `stats.size`로 바이트 크기 확인. Content-Length 계산에 필수 |
| `fs.createReadStream` | 파일을 Readable Stream으로 열기. 대용량 파일도 메모리 부담 없이 처리 가능 |
| `readable.pipe(client)` | Readable → Writable로 데이터 자동 흘리기. backpressure도 자동 처리 |
| `{ end: false }` | pipe 완료 시 소켓을 자동으로 `.end()`하지 않음 — 연결을 유지해서 다음 요청도 처리 가능 |
| `path.join(__dirname, '../static/index.html')` | 절대 경로 — 어느 디렉토리에서 실행해도 올바른 경로를 찾음 |
| `return` 필요 | 비동기 `fs.stat` 콜백이 끝나기 전에 아래 코드가 실행되는 것을 방지 |

#### Content-Length 하드코딩 vs 동적 계산

| 방식 | 코드 | 문제 |
|------|------|------|
| 하드코딩 | `Content-Length: 74` | 파일 수정 시 값 불일치 → 브라우저가 잘린 응답 수신 |
| 동적 계산 | `fs.stat` → `stats.size` | 항상 정확 (이 프로젝트 방식) |

#### 테스트 방법
```bash
curl -H "Host: foo.com:8080" http://127.0.0.1:8080/index.html
curl -H "Host: foo.com:8080" http://127.0.0.1:8080/
```

---

### STEP 6 — 확장 실습 과제

#### 과제 1: 채팅 서버 만들기
```
목표: 여러 클라이언트가 접속해서 서로 메시지를 주고받는 서버
힌트: 접속한 클라이언트 목록을 배열로 관리하고,
      데이터 수신 시 모든 클라이언트에게 브로드캐스트
```

#### 과제 2: 프로토콜 설계
```
목표: JSON 형식의 커스텀 프로토콜 구현
예시: { "type": "message", "data": "Hello" }
      { "type": "ping" }
      { "type": "quit" }
```

#### 과제 3: HTTP 서버로 업그레이드
```
목표: net 모듈로 받은 TCP 데이터를 직접 파싱해서
      HTTP 요청/응답을 수동으로 구현해보기
      (실제 HTTP가 TCP 위에서 어떻게 동작하는지 이해)
```

---

## 주요 개념 정리

### TCP 서버 동작 흐름
```
서버 시작
    │
    ▼
net.createServer() → 서버 소켓 생성
    │
    ▼
server.listen(8080) → 포트 8080에서 대기 (Binded)
    │
    ▼
클라이언트 접속 시 → onConnect(client) 호출 (Connected)
    │
    ▼
client.on('data') → 데이터 수신 시 buffer로 전달
    │
    ▼
buffer.toString() → 문자열로 변환 후 처리
```

### Buffer 이해
```javascript
// 네트워크에서 받은 raw 데이터
const buffer = Buffer.from('Hello');

console.log(buffer);            // <Buffer 48 65 6c 6c 6f>
console.log(buffer.toString()); // 'Hello'
console.log(buffer.length);     // 5
```

### 소켓 이벤트 목록
| 이벤트 | 발생 시점 |
|--------|-----------|
| `connect` | 연결 성공 (클라이언트) |
| `data` | 데이터 수신 |
| `end` | 상대방이 FIN 전송 (연결 종료 시작) |
| `close` | 소켓 완전 종료 |
| `error` | 에러 발생 |
| `timeout` | 타임아웃 발생 |

---

## 더 공부할 것들

### 심화 주제
- **TLS/SSL** — 암호화된 TCP 통신 (`tls` 모듈)
- **UDP 소켓** — `dgram` 모듈로 비연결형 통신
- **WebSocket** — HTTP 업그레이드를 통한 양방향 통신
- **패킷 분석** — Wireshark로 실제 TCP 패킷 캡처 및 분석
  - 이미지의 `soma.pcap` 파일이 바로 이 패킷 캡처 파일

### 관련 Node.js 내장 모듈
| 모듈 | 용도 |
|------|------|
| `net` | TCP 소켓 (이 프로젝트) |
| `dgram` | UDP 소켓 |
| `http` | HTTP 서버 (TCP 위에서 동작) |
| `https` | HTTPS 서버 |
| `tls` | TLS/SSL 암호화 TCP |
| `dns` | DNS 조회 |

### 추천 학습 순서
1. 이 프로젝트 코드 직접 수정해보기 (포트 변경, 메시지 변경)
2. 채팅 서버 과제 구현
3. Wireshark 설치 후 패킷 캡처해서 TCP 3-Way Handshake 직접 확인
4. HTTP 모듈로 이동해서 TCP 기반 고수준 프로토콜 이해
5. WebSocket 학습 (Socket.io 라이브러리)

---

## 참고 명령어 (Windows)

```powershell
# 포트 사용 확인
netstat -ano | findstr :8080

# 프로세스 종료 (PID 확인 후)
taskkill /PID <PID번호> /F

# Node.js 버전 확인
node --version
```
